export const verification_ttl = {
  VERIFICATION_LINK_REDIS_TTL: 300000, // 5 minutes in ms (for Redis)
  VERIFICATION_LINK_JWT_TTL: "5m",
  MAX_ATTEMPTS: 3,
};

export const jwt_ttl = {
  JWT_EXPIRIES_IN: "3d",
};
